<h1> Payment Results </h1>
  A <%= @user_num1 %> year loan of <%= number_to_currency(@user_num2, precision: 0) %>, with an annual interest rate of <%= number_to_percentage(@user_num) %>, requires a monthly payment of <%= number_to_currency(@monthly_payment, precision: 2) %>.
